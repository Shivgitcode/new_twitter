{
  "version": 2,
  "buildCommand": "pnpm run vercel-build",
  "devCommand": "pnpm run dev",
  "installCommand": "pnpm install",
  "builds": [
    {
      "src": "src/bin.ts",
      "use": "@vercel/node"
    }
  ],
  "routes": [
    {
      "src": "/(.*)",
      "dest": "src/bin.ts"
    }
  ]
}
